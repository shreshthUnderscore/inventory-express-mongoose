<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Products</title>
  </head>
  <body>
    <ul>
      <% data.forEach((data) => { %>
      <li><a href="/products/<%= data._id %>"><%=data.itemName %></a></li>
      <% }) %>
    </ul>
    <a href="/products/new">Add New Product</a>
  </body>
</html>
